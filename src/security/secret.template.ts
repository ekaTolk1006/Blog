// Copy file without template tag and provide api key

const apiKey = '<api key>'
const apiProdKey = '<api Prod key>'
const DbKey = '<Db connection string>';

export { 
    apiKey,
    apiProdKey,
    DbKey,
};
